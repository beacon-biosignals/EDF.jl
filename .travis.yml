language: julia
os:
  - linux
dist: xenial
if: branch = master OR tag IS present OR type = pull_request
julia:
  - 1.0
  - 1.3
notifications:
  email: false
matrix:
  allow_failures:
  - julia: nightly
codecov: true
after_success:
  - docs/build.sh
